# =============================================================================
# ShrinkLink — Notification Service Deployment
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: shrinklink
  labels:
    app: shrinklink-notification
    app.kubernetes.io/part-of: shrinklink
    app.kubernetes.io/component: notification
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shrinklink-notification
  template:
    metadata:
      labels:
        app: shrinklink-notification
    spec:
      containers:
        - name: notification-service
          image: <AWS_ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/shrinklink-notification-service:latest
          ports:
            - containerPort: 8004
              protocol: TCP
          resources:
            requests:
              cpu: "50m"
              memory: "96Mi"
            limits:
              cpu: "250m"
              memory: "192Mi"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8004
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8004
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: shrinklink-config
                  key: DB_HOST
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: shrinklink-config
                  key: DB_NAME
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: shrinklink-config
                  key: REDIS_HOST
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: shrinklink-config
                  key: LOG_LEVEL
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: shrinklink-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shrinklink-secrets
                  key: DB_PASSWORD
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shrinklink-secrets
                  key: REDIS_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: shrinklink-secrets
                  key: JWT_SECRET
